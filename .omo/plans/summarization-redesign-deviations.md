# Summarization Redesign — Deviations Log

Deviations, decisions, and adjustments from .omo/plans/summarization-redesign.md execution.

## Task 1 (Wave 1) — Dependency verification

- 2026-09-04: Import smoke test PASSED with the current lockfile (langgraph 1.2.5 / langchain-core 1.4.7 / langchain-openai 1.3.0 / openai 2.41.1). All section 9.7 framework imports (`langchain.agents.create_agent`, `langchain.agents.middleware.{AgentMiddleware,ModelRequest,ModelResponse}`, `langgraph.runtime.Runtime`, `agent.middlewares.summarization.Summarization`, `agent.core`) resolved successfully (exit code 0). No dependency upgrade is needed — PART2 section 16 upgrade subset NOT executed. Evidence: .omo/evidence/task-1-import-smoke.txt
- 2026-09-04: Baseline has one pre-existing failure recorded as-is (not fixed, per task scope): tests/module/test_timer_call_register.py::TestTimerCallRegister::test_reset_cancels_old_generation_task — KeyError: 't1' at runtime/timer_call_register.py:198. Group B verdict = fail; Group A = pass. N_baseline=1854 (1329 + 525 passed). Evidence: .omo/evidence/task-1-baseline.txt, .omo/evidence/task-1-baseline-summary.md
- 2026-09-04 08:55: Environment observation (NOT made by this task): 4 tracked source files were modified by a concurrent process at 08:55:17–08:55:35 — AFTER the baseline run completed (baseline output written 08:55:06, so N_baseline reflects the pre-edit tree). Files: agent/middlewares/humanInTheLoop/core.py, server/service/heartbeat.py, skills/builtin/core/cron/scripts/base.py, tests/module/test_hitl_sandbox_bypass.py (all small type-annotation edits: dict[str,Any]→ToolCall, getattr→direct attr). This task neither made nor reverted these edits (git restore is forbidden by task scope). Baseline numbers remain valid for the tree state they captured.

## Task 2 (Wave 2) — config/num.py rewrite, 28-constant contract

- 2026-09-04: No PART1 §1 vs PART2:530-559 discrepancies found. All 28 contract names are present in PART1 §1 NEW code with identical names; the PART2 import block carries no values, so no value conflicts exist. PART2's contract was verified authoritative post-rewrite via the 28-name import + isinstance script (evidence: .omo/evidence/task-2-const-contract.txt, prints OK). PART2 was designated to win in case of conflict, but its tie-break was never needed.
- 2026-09-04: Transcription adjustment (preservation): PART1 §1 OLD baseline predates `BUS_QUEUE_MAXSIZE` (audit #11, present in the actual current config/num.py). The doc NEW code does NOT explicitly drop it, and the plan says "保留原有 4 个常量" — so the BUS_QUEUE_MAXSIZE block (comment + constant, verbatim from the old file) was preserved, appended after the doc NEW content. This is the only content not taken verbatim from PART1 §1 NEW.
- 2026-09-04: Transcription adjustments (doc-faithful, none made by us): PART1 §1 NEW itself drops the Chinese inline comment on COMPRESS_RATIO and the comment lines under "=== Trigger Thresholds ==="; transcribed verbatim as the doc dictates. Current file pre-rewrite was 9 lines (not the "12 lines" the plan references), but its content = doc OLD baseline (3 constants) + BUS_QUEUE_MAXSIZE, so the transcription source was unambiguous. Final file is ~70 lines (65-line doc NEW block + 5-line preserved BUS_QUEUE_MAXSIZE block) — consistent with the plan's "65 行级终态".
- 2026-09-04: tests/unit/test_config_num.py was added to the commit file list — a TDD addition beyond the original plan Files list (plan only named config/num.py; the user-mandated Red-Green flow requires the test file). 35 tests covering all 28 contract names (values/types from PART1 §1), AUTO_CONTINUE_PROMPT str, PROTECTED_TOOLS collection membership, and the 4 preserved legacy constants + CHARS_PER_TOKEN.
- 2026-09-04: QA tooling note (no code impact): the 28-name and dir() QA scripts ran from the temp dir, so sys.path[0] was the script's own directory and `config` was not importable until PYTHONPATH=<project root> was set. Evidence files were regenerated after the fix. Red evidence (task-2-red.txt) shows ImportError on PREEMPTIVE_TRUNCATE_RATIO as expected; Green shows 35 passed (task-2-green.txt).
