# Summarization Redesign — Deviations Log

Deviations, decisions, and adjustments from .omo/plans/summarization-redesign.md execution.

## Task 1 (Wave 1) — Dependency verification

- 2026-09-04: Import smoke test PASSED with the current lockfile (langgraph 1.2.5 / langchain-core 1.4.7 / langchain-openai 1.3.0 / openai 2.41.1). All section 9.7 framework imports (`langchain.agents.create_agent`, `langchain.agents.middleware.{AgentMiddleware,ModelRequest,ModelResponse}`, `langgraph.runtime.Runtime`, `agent.middlewares.summarization.Summarization`, `agent.core`) resolved successfully (exit code 0). No dependency upgrade is needed — PART2 section 16 upgrade subset NOT executed. Evidence: .omo/evidence/task-1-import-smoke.txt
- 2026-09-04: Baseline has one pre-existing failure recorded as-is (not fixed, per task scope): tests/module/test_timer_call_register.py::TestTimerCallRegister::test_reset_cancels_old_generation_task — KeyError: 't1' at runtime/timer_call_register.py:198. Group B verdict = fail; Group A = pass. N_baseline=1854 (1329 + 525 passed). Evidence: .omo/evidence/task-1-baseline.txt, .omo/evidence/task-1-baseline-summary.md
- 2026-09-04 08:55: Environment observation (NOT made by this task): 4 tracked source files were modified by a concurrent process at 08:55:17–08:55:35 — AFTER the baseline run completed (baseline output written 08:55:06, so N_baseline reflects the pre-edit tree). Files: agent/middlewares/humanInTheLoop/core.py, server/service/heartbeat.py, skills/builtin/core/cron/scripts/base.py, tests/module/test_hitl_sandbox_bypass.py (all small type-annotation edits: dict[str,Any]→ToolCall, getattr→direct attr). This task neither made nor reverted these edits (git restore is forbidden by task scope). Baseline numbers remain valid for the tree state they captured.

## Task 2 (Wave 2) — config/num.py rewrite, 28-constant contract

- 2026-09-04: No PART1 §1 vs PART2:530-559 discrepancies found. All 28 contract names are present in PART1 §1 NEW code with identical names; the PART2 import block carries no values, so no value conflicts exist. PART2's contract was verified authoritative post-rewrite via the 28-name import + isinstance script (evidence: .omo/evidence/task-2-const-contract.txt, prints OK). PART2 was designated to win in case of conflict, but its tie-break was never needed.
- 2026-09-04: Transcription adjustment (preservation): PART1 §1 OLD baseline predates `BUS_QUEUE_MAXSIZE` (audit #11, present in the actual current config/num.py). The doc NEW code does NOT explicitly drop it, and the plan says "保留原有 4 个常量" — so the BUS_QUEUE_MAXSIZE block (comment + constant, verbatim from the old file) was preserved, appended after the doc NEW content. This is the only content not taken verbatim from PART1 §1 NEW.
- 2026-09-04: Transcription adjustments (doc-faithful, none made by us): PART1 §1 NEW itself drops the Chinese inline comment on COMPRESS_RATIO and the comment lines under "=== Trigger Thresholds ==="; transcribed verbatim as the doc dictates. Current file pre-rewrite was 9 lines (not the "12 lines" the plan references), but its content = doc OLD baseline (3 constants) + BUS_QUEUE_MAXSIZE, so the transcription source was unambiguous. Final file is ~70 lines (65-line doc NEW block + 5-line preserved BUS_QUEUE_MAXSIZE block) — consistent with the plan's "65 行级终态".
- 2026-09-04: tests/unit/test_config_num.py was added to the commit file list — a TDD addition beyond the original plan Files list (plan only named config/num.py; the user-mandated Red-Green flow requires the test file). 35 tests covering all 28 contract names (values/types from PART1 §1), AUTO_CONTINUE_PROMPT str, PROTECTED_TOOLS collection membership, and the 4 preserved legacy constants + CHARS_PER_TOKEN.
- 2026-09-04: QA tooling note (no code impact): the 28-name and dir() QA scripts ran from the temp dir, so sys.path[0] was the script's own directory and `config` was not importable until PYTHONPATH=<project root> was set. Evidence files were regenerated after the fix. Red evidence (task-2-red.txt) shows ImportError on PREEMPTIVE_TRUNCATE_RATIO as expected; Green shows 35 passed (task-2-green.txt).

## Task 6 (Wave 5) — §9.7 middleware verbatim transcription

- 2026-09-04: Middleware-side deviations: NONE. `agent/middlewares/summarization.py` = verbatim §9.7 transcription (1262 lines). Hard-req blocks verified in place: `_apply_compression` tail L1069-1074 and `_aapply_compression` tail L1141-1146 (memory_store.load_from_disk() → build_system_prompt(session_id=...) → state_register_mem/db.set_state double write → override_kwargs["system_message"]=SystemMessage(...) → request.override(**override_kwargs)); `need_update_system_prompt: bool = False` stored as `self._need_update_system_prompt` (L420/L427). All 4 plan QA scenarios passed: task-6-hardreq-true.txt (mem='SP'+db='SP'+system_message.content=='SP'), task-6-hardreq-false.txt (no writes, no override), task-6-hardreq-failure.txt (RuntimeError propagates — no try/except, §9.7 locked semantics), task-6-smoke.txt (AgentMiddleware MRO OK, all 14 core methods + module-level _build_static_fallback_summary present, need_update_system_prompt=True accepted). T8 gate: 140/140 (task-6-green.txt).
- 2026-09-04: Test-side adaptations in tests/module/test_summarization_comprehensive.py — first batch (gate 52F→15F). All per plan L573 allowance (obvious fixture/stub errors, §9.7 WINS): dedup ×3 (`_run_non_llm_strategies` returns `(messages, reduced)` tuple → unpack + marker fmt args); prune ×3 (fixture out_chars adapted to PRUNE_PROTECT_TOKENS=40000 / PRUNE_MIN_REDUCTION_TOKENS=5000, protected-tool naming); aggressive ×3 (`_aggressive_truncate(messages)` single-arg); truncate_only → renamed `test_preemptive_truncate_shrinks_tool_output` calling `_preemptive_truncate` directly (3000-char content → head 600+tail 600+marker 26, omitted=1800); llm_summary `_StubLLM`→suite StubModel; capture ×2/inject ×1 (`_capture_recovery_context` returns dict {user_intent, file_ops, previous_file_ops}, `_inject_recovery_context(messages, ctx, session_id)`); monitor resets/caps (state-key asserts); is_empty real_summary (AIMessage wrap); static fallback ×7 (`_build_static_fallback_summary(messages: list)`); extract ×3 (returns `{"read_files": [...], "modified_files": [...]}`); format/parse ×5 (dict in/out, FILE_OPS_LIST_MAX_CHARS=900 FIFO); acreate error/sufficient (§9.7 catches ALL exceptions → static fallback, nothing propagates); aapply noop (`_record_compression` runs on EVERY apply → count==1 even on noop) + llm path range(50)→range(100); awrap missing-session → RuntimeError.
- 2026-09-04: Test-side adaptations — final batch (gate 15F→0F, 140/140). Precise list (file+line at edit time):
  1. L362 test_reported_tokens_zero_without_total_key: pydantic requires total_tokens in usage_metadata at construction → valid AIMessage + `.model_copy(update={"usage_metadata": {...}})` (bypasses validation). Fixture-construction error.
  2. L1167 test_apply_compression_non_llm_sufficient_strategy: expected `"non_llm_sufficient"` → `"non_llm"` (§9.7: gentle branch keeps "non_llm"; "non_llm_sufficient" is a dead `or`-fallback when non_llm_reduced>0, line 1050).
  3. L1183 test_apply_compression_skip_llm_fallback + L1680 test_aapply_compression_fallback_when_skip_llm: fallback headers live INSIDE one AIMessage content → header-in-list assertion replaced by `any("## Latest Unresolved User Request" in m.content ...)`.
  4. L1242 test_monitor_degradation_counts_empty_responses: bare str "" has no .content → `_is_empty_response(str)` is False → counter never increments; fix `AIMessage(content="")` ×3 (§9.7: "empty" = blank str content of a response OBJECT).
  5. L1258/L1269 monitor resets/caps: same str→AIMessage wrap (were green by accident; strengthened to real intent, attempts==2 still caps force).
  6. L1279 test_is_empty_response_noop_summary_is_empty → renamed test_is_empty_response_blank_content_is_empty: §9.7 has NO noop-summary pattern detection, blank-content check only. Intent conflict — §9.7 WINS.
  7. L1294 test_is_empty_response_empty_string_is_empty: str "" → AIMessage(content="").
  8. L1628 test_acreate_summary_empty_text_passthrough: StubModel(text="") hits falsy `text or default` → 94-char default (fallback never triggered); fix StubModel(text="too short") (truthy, <50 chars → §9.7 short-text fallback).
  9. L1796/1807/1820/1867/1941 awrap tests: sync handlers/lambdas → `async def` handlers (§9.7 awrap awaits the handler; sync callable → TypeError "object str can't be used in 'await' expression" at summarization.py:1260).
  10. L1848 test_awrap_model_call_llm_path_async_stub: async handler + range(50)→range(100) — 50 turns est ≈5600 < budget 10000 → cutoff==0 → noop → stub.calls==0; 100 turns est ≈10800 ≥ trigger 5000 and > budget → cutoff>0 → ainvoke fires.
  11. L1933 test_wrap_model_call_idempotent_noop_on_second_pass: no compression ran → `_COMPRESSION_COUNT_KEY` never set → get_state returns None (`None == 0` is False) → `assert not get_state(...)`.
- 2026-09-04: Commit file list deviation: tests/module/test_summarization_comprehensive.py is committed alongside agent/middlewares/summarization.py (plan Files list names only the middleware). Rationale: T6's mandated test-side fixes (plan L573) are part of the TDD Green deliverable; committing only the middleware would leave the previously committed red-baseline assertions (f350ea8) failing against the new middleware in-repo (bisect hazard). Precedent: Task 2 added tests/unit/test_config_num.py to its commit with the same justification.
